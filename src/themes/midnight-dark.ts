import { Defaults, BaseColors, TextColors, Theme } from "../templates";

const defaults: Defaults = {
    name: "midnight-dark",
    type: "dark",
};

const baseColors: BaseColors = {};
export const theme: Theme = {
    defaults: defaults,
};
